# fraud-detection-app
A machine learning app to detect fraudulent transactions with a web interface (Gradio)
# 💳 Bank Transaction Fraud Detector

A machine learning web app built with **Gradio** to detect fraudulent bank transactions based on **30 input features**.

## Features
- Logistic Regression model trained on real dataset
- Preprocessing with StandardScaler
- Interactive **Gradio interface**
- Probability pie chart for better visualization

## Installation
```bash
pip install -r requirements.txt
python fraud_predictor_banking
